#!/usr/bin/env python3
import sys
import webbrowser

if len(sys.argv) != 2:
    print("Usage: cdoc <crate_name>", file=sys.stderr)
    sys.exit(1)

crate = sys.argv[1]
webbrowser.open(f"https://docs.rs/{crate}")
