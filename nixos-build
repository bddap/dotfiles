#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")"

flake="path:$(realpath nix/nixos)#default"

nixos-rebuild build --flake "$flake"

if [ -n "${1+x}" ]; then
	sudo nixos-rebuild "$1" --flake "$flake"
fi
